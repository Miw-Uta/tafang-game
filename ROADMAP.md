# 甜橙谷技术路线

## 当前阶段：内容注册与战局会话

- 地图、事件、敌人、模式和关卡内容集中在 `game-content.js`，由 `ContentRegistry` 校验和索引。
- `GameSession` 统一管理无尽/闯关模式、地图、波次、生命和结束条件；当前已准备三章闯关数据。
- 每张地图拥有独立路径，碰撞、移动和绘制均从当前地图定义读取。
- 特效使用统一的 `hits` 队列，并设置上限，避免后期多塔、多敌人时无限堆积造成卡顿。
- 音效通过 `audioBus` 统一触发。当前使用 Web Audio 合成短音，后续可替换为音频资源而不改战斗逻辑。
- 进化弹窗、暂停状态和主路线/分支状态分离，后续可扩展为完整进化树节点。
- 隐藏塔采用特殊摆放配方驱动，并记录到本局进化发现列表，便于继续扩展组合探索。

## 下一阶段：应用层拆分

1. 把塔内容表迁入独立内容模块，并按基础塔、路线、分支、融合配方拆分。
2. 将 `game.js` 的 Canvas 绘制提取为 renderer，将 DOM 更新提取为 view，将输入提取为 controller。
3. 增加关卡选择界面、章节解锁和存档；运行时状态与存档状态保持单向同步。
4. 将特效拆成可注册 emitter：投射物、命中特效、状态环、击退轨迹和 Boss 动画分别管理生命周期。

## 适合迁移到 Vite + TypeScript 的时机

当项目出现多个关卡模块、超过约 20 个可编辑数据文件，或需要音频/图片批处理、单元测试和热更新时，再引入 Vite + TypeScript。迁移顺序建议是：

1. 先把数据表和战斗计算提取为无 DOM 的纯模块。
2. 为合成、伤害、状态效果和关卡波次补单元测试。
3. 再迁移 Canvas 渲染、输入和 UI，最后接入资源管线。

当前先不引入框架依赖，保持双击或 Python 静态服务器即可运行，减少开发环境成本。
