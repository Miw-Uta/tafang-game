# 平衡与策略规则

验收关注三件事：每个主路线都有能兑现的作用，收益有代价，失败后能通过调整布阵改善。自动通关只是回归工具，不能当作真人胜率。

## 本轮修正

- 战役 85 波固定敌群与词缀。首领只在指定位置出现，侦察报告和实际生成共用同一份编排。
- 五行主路线稳定可选，稀有路线按关卡与波次轮换，所属谱系的分支完整开放，允许预先规划反制与羁绊。
- 影刃改为攻击射程内实际目标，四段合计一次面板伤害；解决合法部署位上无法命中的问题。
- 冻结不再同时触发眩晕，击退先检查免疫。首领眩晕结束后有 1.5 秒恢复窗口，生息庭对控制时长的加成正确生效。
- 战中移动和重部署有 2 秒架设时间，保留更长的剩余冷却，取消未发出的攻击。整备移动自由。
- 培育跨阈值会升级，升级到 5/10 级进入进化；无合法培育目标时保留资源。20 灵力转换只改变资源类型，不能跳过等级序列。
- 首关初始守卫从 Lv.1 调为 Lv.3，降低不同开局支援的难度落差。
- 灵种支援调整为额外 2 枚 Lv.2 灵种；伤害和战技支援不再被更高等级灵种直接压过。首关三种支援 × 两种剧情选择均可通关且不漏怪。
- 侦察提示现在给出具体谱系建议，例如重甲推荐木/金/土，霜月抗性波推荐金/火/土输出；“反制”会明确提示优先锁定祭司光环或护盾目标。

## 压力曲线

基础关卡数值继续保留独立敌群、路线、词缀和事件，再按章节修正生命。七幕倍率依次为 `1 / 1.5 / 2.1 / 3 / 3.25 / 3.5 / 4`。标准难度按该曲线运行，精锐在此基础上把敌军生命提高到 `+45%`。剧情选择仍按各自说明生效。所有阵容面对相同编排和参数。

调整依据：规则修复后的初次对照中，霜月关和终关的六类阵容均无伤。压力实验随后确认，增加中后期压力可以显露控制、护盾和输出构成的差异，混合阵容仍能通过。

## 可复现实验

```bash
npm test
npm run simulate -- --seed 42
npm run simulate -- --support power --choice second --seed 20260907
npm run simulate -- --support charge --choice second --seed 42
npm run balance
```

对照使用标准难度、灵种支援、均衡成长、第一项剧情选择，允许战技，不使用转换。固定算法会布阵、领取补给、合成和使用实际提供的进化。单系倾向仍保留关卡自带守卫，不能把它视为完全单一谱系。累计生命损失不会被修复补给抹除。

关键对照（种子 42）：

| 关卡 | 混合 | 偏火 | 偏水 | 偏金 | 偏木 | 偏土 |
| --- | --- | --- | --- | --- | --- | --- |
| 霜月关 | 胜 / 损失 0 | 胜 / 0 | 败 / 11 | 胜 / 0 | 胜 / 0 | 胜 / 0 |
| 世界树之心 | 胜 / 损失 0 | 败 / 10 | 败 / 10 | 胜 / 8 | 胜 / 5 | 胜 / 0 |

精锐混合阵容（种子 42、灵种支援）霜月关与世界树之心均通关且累计漏怪为 0；这表示精锐更适合作为熟练玩家的完整反制考验，不能用单一属性一路推进。

完整结果写入 `artifacts/balance-audit.json`，含运行时源码哈希、每次进化的可选项、最终阵容与逐波资源。运行新的审计会覆盖这份生成文件。

## 已知边界

混合算法偏向覆盖面较大的部署位，目前仍能在多关无伤，尚不能证明每种分支、融合和羁绊都有相同价值。自动算法也不会完整利用手动集火与地图目标，因此不用于证明所有三星挑战可达。后续真人测试应记录首见玩家失败波次、选择分布、漏怪类型和是否理解反制，重点关注精锐难度、后期分支与融合的投入回报。
