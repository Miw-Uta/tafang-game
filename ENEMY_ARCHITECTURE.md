# 敌人系统架构

## 目标

敌人系统将“敌人是什么”“这一只敌人当前如何”“本波生成哪一种敌人”分开。新增族类、词缀和被动能力应以配置或独立策略为主，不应继续扩大 `game.js` 的主循环。

## 领域对象

- `EnemyDefinition`：不可变族类蓝图，保存身份、阶级、基础倍率、解锁波次、能力标签和固有修正。
- `TraitDefinition`：不可变词缀蓝图，只描述可组合的属性修正。
- `DefinitionCatalog`：负责蓝图注册、唯一性和查询。
- `Enemy`：战场实体，拥有生命、护盾、抗性、状态、移动、受伤和逃脱行为。
- `StatusController`：统一异常状态时长、来源和控制抗性换算。
- `EnemyFactory`：只允许从已登记蓝图创建合法实例，并组合词缀。
- `SpawnDirector`：根据波次、位置、解锁条件和权重决定普通、精英与 Boss 阵容。

## 数据流

```text
wave context -> SpawnDirector -> EnemyFactory -> Enemy
                                         |        |
                          immutable definitions   +-> update / receiveDamage
                                                  +-> status and shield state
```

塔只通过敌人的稳定战斗接口施加伤害和状态。界面可以读取兼容字段，但不负责修改敌人的规则。

## 当前族类

| 族类 | 阶级 | 战斗身份 |
| --- | --- | --- |
| 苔芽精 | 普通 | 基础群体单位 |
| 棘背兽 | 普通 | 低速轻甲单位 |
| 流光蛾 | 普通 | 高速控制抗性单位 |
| 岩壳卫 | 精英 | 护甲与独立护盾前锋 |
| 沼泽祭司 | 精英 | 持续再生单位 |
| 腐根暴君 | Boss | 护盾与半血狂暴首领 |
| 烬角鹿王 | Boss | 高速、护甲与控制抗性首领 |

## 扩展约束

主动技能下一阶段通过行为策略注册表接入，例如分裂、治疗光环、召唤、冲刺和阶段转换。技能不得直接依赖 DOM 或 Canvas；表现层通过领域事件播放动画和提示。
