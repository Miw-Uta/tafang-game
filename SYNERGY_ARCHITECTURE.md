# 阵容羁绊架构

## 双标签阵容

每座塔同时参与两个维度：

- **阵营**：金、火、土属于烈铸盟；木、水、风属于生息庭；阴、阳、雷属于天象仪。
- **职业**：按攻击模式映射为破军（单体）、贯星（穿透）、引脉（连锁）、燎原（范围）或神谕（全域）。

同一座塔可以同时激活阵营和职业羁绊。系统按 `evo` 形态去重：两座完全相同的塔只贡献一次计数，不同 Lv.10 分支会分别计数。这样现有九条路线和七组分支本身就是一张阵容矩阵，不依赖持续增加基础元素。

融合塔可以拥有多个阵营标签。例如焚木共鸣塔同时属于烈铸盟和生息庭，霜雷塔同时属于生息庭和天象仪。它们因此既是高阶战力，也是连接两套阵容的转型枢纽。

## 领域边界

`synergy-system.js` 提供三个领域对象：

- `SynergyDefinition`：不可变羁绊蓝图，保存标签、作用范围与多档门槛。
- `SynergyCatalog`：注册和校验羁绊定义。
- `SynergySystem`：按唯一形态统计羁绊，为单座塔计算伤害和冷却修正。

羁绊内容配置位于 `game-content.js`，塔到标签的映射位于 `game.js` 的组装层。战斗实体只接受最终冷却倍率，不依赖任何具体羁绊名称。

## 扩展方式

新增普通塔时，优先复用已有阵营和职业；一个新形态会自然参与两个维度。新增混合融合塔时，在 `fusionFactions` 中声明两个阵营即可。

新增羁绊只需在 `GameContent.synergies` 注册：

```js
storm: {
  name: '风暴议会', icon: '※', tag: 'faction:storm', scope: 'members',
  desc: '风暴成员提高攻击速度',
  tiers: [{ count: 2, attackSpeed: .1 }, { count: 4, attackSpeed: .24 }]
}
```

`scope: 'members'` 只强化拥有该标签的塔；`scope: 'all'` 强化整支军团。当前支持 `damage` 和 `attackSpeed`，未来增加护盾、控制增幅或击杀触发时，应扩展结构化效果字段和对应战斗策略，不在 UI 文案中解析规则。
