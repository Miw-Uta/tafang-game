# 进化系统架构

## 进化节点

进化不是普通等级的附加字段，而是等级曲线上的两个决策节点：

- `Lv.5`：基础塔选择九条主路线之一，写入 `evolutionPath`，塔进入 `evoTier = 1`。
- `Lv.10`：从已选主路线的七个分支中选择一个，塔进入 `evoTier = 2`。
- `Lv.20`：完成路线终点。融合塔属于独立结果，不进入普通分支流程。

## 培养制成长

塔有两种培养动作：

- **共鸣合成**：同等级、同形态塔合并，获得被吞噬等级 + 50% 共鸣奖励，效率最高。
- **培养吞噬**：低等级、同形态塔被主塔吸收，只按被吞噬等级转化灵蕴，效率较低但不需要等待配对。

两种动作都写入主塔的 `growth` 灵蕴。当前升级门槛是 `8 + 等级 × 2`，随进度平滑增长而非指数增长；达到门槛后主塔提升一级并结算剩余灵蕴。自动整编始终优先共鸣合成，再考虑培养吞噬。

此外，玩家可以主动开启“灵蕴祭炼”，把任意其他塔献祭为通用灵蕴。跨路线祭炼只有约 60% 基础转化率，进化阶位和融合形态只提供小幅补偿，因此它是一个有成本的单核突破选择，不会替代共鸣合成。

培养系统以“整波方针”取代逐枚素材操作。玩家可以在战斗中随时规划三种长期策略；当前波的战斗加成会锁定，预案在波末自动结算并成为下一波方针，不能通过卡秒切换套利：

- **共生培育**：自动把库存分给当前成长进度最低的守卫，并把所有已激活羁绊效果提高 25%。没有组成羁绊时不会获得无条件增伤。
- **主脉培育**：库存只灌注指定核心；核心伤害提高 30%，其他塔伤害降低 10%。这是快速突破节点与阵容韧性之间的交换。
- **封存共鸣**：库存完全不消耗；每封存 20 点灵蕴提供 2% 全队伤害，上限 20%。玩家需要持续战力时可以囤积，但会放弃永久等级成长。

每波结算后有 4 秒可观察自动分配结果，随后自动开始下一波，也可点击“提前开波”跳过倒计时。到达 Lv.5 或 Lv.10 时进化选择会暂停游戏与倒计时。仓库的“灌注至节点”和跨路线“灵蕴祭炼”仍然存在，但定位是少量纠偏，并会在 Lv.5、Lv.10、Lv.15 或 Lv.20 停止，不再承担日常成长主循环。

仓库压缩保持素材价值：Lv.1 到 Lv.5 灵种分别提供 1、2、4、8、16 点灌注灵蕴。指数压缩只存在于 Lv.5 以下的库存整理，不参与塔的 Lv.5-20 成长曲线。

这让每一级都是真实等级和真实进度。高等级塔可以回收低等级塔，低等级产物不会因为无法凑成同级对子而报废，通往 Lv.20 的素材需求从指数级降为近似线性。

## 领域对象

`evolution-system.js` 提供：

- `EvolutionDefinition`：不可变路线/分支蓝图。节点会自动推导 `origin / primary / branch / fusion` 阶段，也可以通过 `stage` 显式声明。
- `EvolutionGraph`：由蓝图构建的只读谱系图，提供根节点、子节点、完整祖链和后代查询。
- `EvolutionDecision`：一次进化决策的事务会话。它冻结候选上下文，统一负责校验和提交，并拒绝重复提交。
- `EvolutionTree`：目录与规则门面，负责注册蓝图、识别塔当前节点、创建决策会话，以及保留旧版 `choices/validateChoice` API。

`game.js` 只负责把塔状态和界面交给领域规则，不再自行修改进化状态。阵容羁绊由 `synergy-system.js` 解析，进化分支通过形态、阵营和职业标签参与羁绊，不侵入进化决策事务。

### 扩展约定

新增路线时只需注册一个定义，并设置 `parent`、`stage`、`tags` 或自定义条件元数据；分支会自动挂接到谱系图。需要复杂解锁条件时，在 `EvolutionTree.validateChoice` 外组合策略即可，避免把 UI 文案、随机抽取和状态变更耦合在一起。
