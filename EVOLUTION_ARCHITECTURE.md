# 进化系统架构

## 进化节点

进化不是普通等级的附加字段，而是等级曲线上的两个决策节点：

- `Lv.5`：基础塔选择九条主路线之一，写入 `evolutionPath`，塔进入 `evoTier = 1`。
- `Lv.10`：从已选主路线的七个分支中选择一个，塔进入 `evoTier = 2`。
- `Lv.20`：完成路线终点。融合塔属于独立结果，不进入普通分支流程。

## 合成制成长

培育不再是独立资源系统。塔的永久成长只有一个入口：合成完全相同的形态、进化路线和分支。

- **同级共鸣**：两座同级同形塔合并，主塔至少提升一级，并保留此前积累的合成进度。
- **素材合成**：较低等级的同形塔可以并入高等级主塔，提供 `ceil(2 + 素材等级 × 1.25)` 点合成进度。

主塔每级门槛为 `8 + 当前等级 × 2`。这让低等级产物始终有价值，同时避免从 Lv.15 到 Lv.20 再次退化成大量重复点击或 `2^n` 的素材墙。跨形态献祭不存在，因此不同路线不会被无成本转化为单核等级。

## 纵向与横向

同一份塔资产只有两个互斥用途：

- 复制相同路线和分支，继续合成主力塔，获得纵向强度。
- 保留不同形态，组成阵营与职业羁绊，获得横向阵容能力。

羁绊只按唯一形态计数，重复塔不会虚增层数。进化选择因此同时决定未来的合成配对和阵容标签，而不是一个脱离战斗的数值菜单。

分支使用统一的攻击预算生成数值。单体、连锁、穿透、溅射和全域攻击按预期目标数折算，切换攻击方式时会重新分配单目标伤害；分支只获得约 10%-35% 的总预算提升，不能同时凭空获得更高单发和更多目标。多段武器也共享一次攻击预算，避免动画段数成为隐藏伤害倍增器。

## 仓库与事务

世界树只负责按根系策略生成灵种。灵种进入仓库后不会自动压缩，波末也不会自动注入任何塔。玩家在整备阶段点击一次“合成”，系统才会：

1. 连锁整理仓库中的 Lv.1-Lv.4 同级对子，最高停在 Lv.5。
2. 合并战场上的同形塔，优先处理不会触发决策的组合和同级共鸣。
3. 到达 Lv.5 或 Lv.10 时暂停并打开进化选择，不跨过未确认节点。

一次操作开始前会保存战场塔快照与完整仓库计数；撤销会同时恢复两者，避免快捷操作误伤阵容。手动部署、移动、撤回或进化后会使旧快照失效。

波次完成后仍保留 4 秒整备倒计时并自动开始下一波，但结算只追加灵力和灵种，不执行隐式合成。

## 领域对象

`evolution-system.js` 提供：

- `EvolutionDefinition`：不可变路线/分支蓝图。节点会自动推导 `origin / primary / branch / fusion` 阶段，也可以通过 `stage` 显式声明。
- `EvolutionGraph`：由蓝图构建的只读谱系图，提供根节点、子节点、完整祖链和后代查询。
- `EvolutionDecision`：一次进化决策的事务会话。它冻结候选上下文，统一负责校验和提交，并拒绝重复提交。
- `EvolutionTree`：目录与规则门面，负责注册蓝图、识别塔当前节点、创建决策会话，以及保留旧版 `choices/validateChoice` API。

`game.js` 只负责把塔状态和界面交给领域规则，不再自行修改进化状态。阵容羁绊由 `synergy-system.js` 解析，进化分支通过形态、阵营和职业标签参与羁绊，不侵入进化决策事务。

### 扩展约定

新增路线时只需注册一个定义，并设置 `parent`、`stage`、`tags` 或自定义条件元数据；分支会自动挂接到谱系图。需要复杂解锁条件时，在 `EvolutionTree.validateChoice` 外组合策略即可，避免把 UI 文案、随机抽取和状态变更耦合在一起。
